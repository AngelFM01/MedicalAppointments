info:
  name: getEmergencyContactsPaged
  type: http
  seq: 2

http:
  method: GET
  url: https://localhost:7112/EmergencyContacts/paged?pageNumber=1&pageSize=10&filter=PacienteId == 1 AND Activo == true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
